import React from 'react';
import GenerateToken from '../Components/GenerateToken';

const Dashboard = () => {
  return (
    <div>
      <h2>Dashboard</h2>
      <GenerateToken />
    </div>
  );
};

export default Dashboard;
